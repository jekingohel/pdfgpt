version: 1.0
runtime: python3
build:
  commands:
    build:
      - virtualenv venv
      - source venv/bin/activate
      - pip install -r requirements.txt
run:
  command: gunicorn app:app
  network:
    port: 8000
